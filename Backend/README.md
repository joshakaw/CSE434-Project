# Set up

Before you can run:
- The `model.safetensors` file must be inserted into `classifier\models\healthguide_model`. It was disincluded from source control because it exceeds GitHub's 100MB limit.
- The `secrets.json` file must be added to the root of `Backend`. This contains the API key for Gemini.